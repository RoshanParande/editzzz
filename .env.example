# Local dev API endpoint used by Vite proxy.
# Change this only if your backend runs on a different host/port.
VITE_PROXY_TARGET=http://127.0.0.1:3000

# Optional: force frontend fetch() to use a full backend URL instead of relative /api.
# Leave empty for normal local proxy usage.
VITE_API_BASE_URL=